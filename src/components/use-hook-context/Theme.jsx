import ThemeCard from "./ThemeCard";
import ThemeProvider from "./ThemeProvider";

const Theme = () => {
  return (
    <ThemeProvider>
      <ThemeCard />
    </ThemeProvider>
  );
};

export default Theme;
